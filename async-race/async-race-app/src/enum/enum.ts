export enum SortTypes {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum SortCategory {
  wins = 'wins',
  time = 'time',
}

export enum Pages {
  garage = 'garage',
  winners = 'winners',
}

export enum RaceStatus {
  start = 'start',
  process = 'process',
  finished = 'finished',
}
